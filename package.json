{
  "name": "ai-swarm-v7",
  "version": "7.0.0",
  "private": true,
  "description": "AI Swarm Lab — 31 agentes especializados",
  "scripts": {
    "dev":     "vite --port 3000",
    "build":   "vite build",
    "preview": "vite preview --port 3000",
    "start":   "vite preview --port ${PORT:-3000} --host 0.0.0.0"
  },
  "dependencies": {
    "react":                "^18.3.1",
    "react-dom":            "^18.3.1",
    "vite":                 "^5.4.0",
    "@vitejs/plugin-react": "^4.3.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
